info:
  name: Get all threads
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/threads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
