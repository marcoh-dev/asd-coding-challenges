info:
  name: Get comment by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/comments/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
