info:
  name: Get bookmark by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/bookmarks/:id"
  params:
    - name: id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
