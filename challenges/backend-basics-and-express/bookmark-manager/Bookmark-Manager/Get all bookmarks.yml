info:
  name: Get all bookmarks
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/bookmarks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
