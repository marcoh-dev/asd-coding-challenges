info:
  name: Delete a bookmark
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/bookmarks/:id"
  params:
    - name: id
      value: "9"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
