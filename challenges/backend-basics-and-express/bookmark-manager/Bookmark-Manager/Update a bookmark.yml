info:
  name: Update a bookmark
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseURL}}/bookmarks/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Google 12",
        "tag": "googl Engine"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
