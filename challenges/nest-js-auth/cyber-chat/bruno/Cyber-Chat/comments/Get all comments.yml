info:
  name: Get all comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
