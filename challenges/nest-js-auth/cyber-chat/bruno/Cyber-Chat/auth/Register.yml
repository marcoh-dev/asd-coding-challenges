info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"test1",
        "password":"12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
